{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "apps/backend/**/*.js": "npm --prefix apps/backend run lint",
    "apps/frontend/**/*.{ts,tsx}": "npm --prefix apps/frontend run lint"
  }
}
